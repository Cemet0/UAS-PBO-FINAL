/* ==========================================================================
   KONFIGURASI EMBERLORD — Sesuaikan dengan environment Anda
   ==========================================================================
   Salin file ini ke config.local.js untuk konfigurasi lokal (tidak ter-track git),
   atau ubah langsung nilai di bawah ini untuk development.
   ========================================================================== */

const CONFIG = {
    API_BASE_URL: 'http://localhost:8080/api',

    GITHUB_TOKEN: '',
    GITHUB_MODEL: 'llama3.2',
    GITHUB_API_URL: 'http://localhost:11434/v1/chat/completions',
};
